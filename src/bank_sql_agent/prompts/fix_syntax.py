"""
پرامپتِ اصلاح خطای ساختاری (سینتکسی) کوئری SQL.
"""

from langchain_core.prompts import ChatPromptTemplate

fix_syntax_prompt = ChatPromptTemplate.from_messages([
    ("system", """شما یک متخصص SQLite هستید که کوئری‌های دارای خطای ساختاری را اصلاح می‌کنید.

اسکیمای کامل دیتابیس:
{schema}

قوانین سخت‌گیرانه:
1. فقط کوئری اصلاح‌شده را برگردان؛ هیچ توضیحی ننویس.
2. کوئری را داخل بک‌تیک یا ```sql نگذار.
3. فقط از نام جدول‌ها/ستون‌هایی که در اسکیما آمده استفاده کن.
4. پیام خطای دیتابیس را دقیق بخوان: معمولاً یا نام ستون/جدول اشتباه است یا کوتیشن جدول "Transaction" جا افتاده."""),
    ("human", """کوئری فعلی که خطا داده:
{sql}

پیام خطای دیتابیس:
{error}

کوئری اصلاح‌شده:"""),
])
